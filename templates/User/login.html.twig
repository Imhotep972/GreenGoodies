{% extends 'base.html.twig' %}

{% block title %}Connexion - {{ parent() }}{% endblock %}


{% block body %}

			<!-- Début section CONNEXION -->
			<section class="gg-connexion">
				<article class="h-100 w-100">
					<div class="container-fluid h-100 w-100">
						<div class="row h-100">
							<div class="col-12 col-md-6 p-0 ">
								<img src="{{ asset('images/connexion_img.webp') }}" alt="Images" class="w-100 h-100 image_fluid"/>
							</div>
							<div class="col-12 col-md-6 mt-4 d-flex justify-content-center">
								<form class="w-75 px-4"  method="post">
                                    {% if error %}
                                        <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
                                    {% endif %}

                                    {% if app.user %}
                                        <div class="mb-3">
                                            Vous êtes connecté en tant que {{ app.user.userIdentifier }}, <a href="{{ logout_path() }}">Se deconnecter</a>
                                        </div>
                                    {% endif %}

									<h2 class="my-5">Connexion</h2>
									<div class="mb-3">
                                        <label for="username"  class="form-label fs-6">Adresse email</label>
                                        <input type="email" value="{{ last_username }}" name="_username" id="username" class="form-control customInput" autocomplete="email" required autofocus>
									</div>
									<div class="mb-3">
                                        <label for="password"  class="form-label fs-6">Mot de passe</label>
                                        <input type="password" name="_password" id="password" class="form-control customInput" autocomplete="current-password" required>
									</div>
                                    <input type="hidden" name="_csrf_token" data-controller="csrf-protection" value="{{ csrf_token('authenticate') }}">
									<button type="submit" class="btn custom-btn">Se connecter</button>
									<div class="w-100 text-center">
										<a href="{{ path('app_account_register') }}" class="fs-6 fw-normal">Pas encore de compte ? S'inscrire</a>
									</div>
								</form>
							</div>	
						</div>
					</div>
				</article>
			</section><!-- Fin Section CONNEXION -->

{% endblock %}
